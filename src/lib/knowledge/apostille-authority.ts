/**
 * Кто ставит апостиль и действует ли территориальный признак.
 *
 * Источник — «Рабочая тетрадь стажёра» компании, страница 10 (разбор:
 * docs/bot-audit/04-trainee-notebook.md §1). Это внутренний обучающий материал,
 * по которому работают люди: таблица согласована, снабжена исключениями и
 * сносками. Она первична по отношению к 1723 правилам, извлечённым из
 * инструкций поштучно.
 *
 * Зачем отдельный модуль, а не правило в базе. Территориальность — это НЕ факт
 * о мире, который можно процитировать, а РЕШЕНИЕ: «можем ли мы взять этот
 * документ». Пока оно принимается поиском по смыслу, ответ зависит от того,
 * какой абзац окажется ближе к вопросу. Правило R-963 про Саратов в базе есть,
 * и бот всё равно отвечал неверно — маршрут до него не доходил.
 *
 * Главное, ради чего это написано: у образовательного комитета территориального
 * признака НЕТ. Формулировка «апостиль ставится только в регионе выдачи,
 * исключений нет», записанная в раннем диагнозе, неверна и стоила бы заказов:
 * диплом, выданный в Саратове, мы апостилируем в Петербурге законно.
 */

export type ApostilleAuthorityKey = 'min_justice' | 'zags' | 'mvd' | 'education';

export interface ApostilleAuthority {
  key: ApostilleAuthorityKey;
  label: string;
  /**
   * Действует ли территориальный признак: апостилируются только документы,
   * выданные в регионе обращения.
   */
  territorial: boolean;
  /** Срок, как он назван в тетради. Не выдумывать и не пересчитывать. */
  deadline: string;
  /** Нужна ли доверенность. */
  powerOfAttorney: string;
  /** Сноска тетради — то, что предлагать, когда прямой путь закрыт. */
  footnote?: string;
}

export const APOSTILLE_AUTHORITIES: Record<ApostilleAuthorityKey, ApostilleAuthority> = {
  min_justice: {
    key: 'min_justice',
    label: 'Министерство юстиции',
    territorial: true,
    deadline: 'стандартные документы — 3 рабочих дня; документы с редкой подписью чиновника (например судебные) — 30 и более рабочих дней',
    powerOfAttorney: 'без доверенности, без записи, в рабочие часы',
    footnote:
      'Если документ нельзя подать на апостиль в оригинале, но с него можно снять нотариальную копию — предложить клиенту этот вариант.',
  },
  zags: {
    key: 'zags',
    label: 'Архив ЗАГС',
    territorial: true,
    deadline: 'по записи по документу, плюс 5 дней на процедуру',
    powerOfAttorney: 'без доверенности',
    footnote: 'Документы, выданные в Ленинградской области, апостилируются в отдельном комитете.',
  },
  mvd: {
    key: 'mvd',
    label: 'Министерство внутренних дел',
    territorial: true,
    deadline: 'без записи, плюс 5 дней на процедуру',
    powerOfAttorney: 'без доверенности именно на апостиль',
    footnote:
      'Через МВД апостилируются ТОЛЬКО справки о несудимости на синем бланке. Другие документы МВД — только через Министерство юстиции: в оригинале, если он подходит по признакам, либо через нотариальную копию.',
  },
  education: {
    key: 'education',
    label: 'Образовательный комитет',
    // ЕДИНСТВЕННОЕ исключение из территориальности во всей таблице.
    territorial: false,
    deadline: 'официальный срок комитета 45 рабочих дней; на практике 2–3 недели',
    powerOfAttorney: 'официально по доверенности, но есть вариант без неё',
    footnote:
      'Скорость зависит от ответа ВУЗа на запросы комитета. Без приложения к диплому или аттестату возможен отказ.',
  },
};

/**
 * Типы документов по органам. Порядок проверки важен: справка о несудимости
 * упоминает слово «справка», которое встречается и у ВУЗов, поэтому МВД
 * проверяется раньше.
 *
 * Границы слов заданы через отрицательный просмотр назад по буквам: `\b` в JS
 * опирается на латиницу и перед кириллицей не срабатывает.
 */
const NOT_LETTER_BEFORE = '(?<![А-Яа-яЁёA-Za-z])';
const NOT_LETTER_AFTER = '(?![А-Яа-яЁёA-Za-z])';

const DOCUMENT_MATCHERS: Array<{ authority: ApostilleAuthorityKey; pattern: RegExp }> = [
  {
    // ПЕРВЫМ и намеренно. Нотариальная копия и нотариальный перевод — сами по
    // себе нотариальные документы, и апостилирует их Минюст, независимо от
    // того, копия чего это. Без этой ветки «апостиль на нотариальный перевод
    // диплома» попадал в образовательный комитет по слову «диплом» и получал
    // ответ «да, можем, территориального признака нет» — тогда как у Минюста
    // признак есть. Неверный детерминированный ответ, воспроизведён.
    authority: 'min_justice',
    pattern: /нотариальн[а-яё]*\s+(?:копи|перевод)/iu,
  },
  {
    authority: 'mvd',
    // «СОН» — внутреннее сокращение справки об отсутствии судимости. Границы
    // с обеих сторон обязательны и РАЗНЫЕ: слева просмотр назад, справа вперёд.
    // Один и тот же просмотр назад с обеих сторон проверял бы букву «н» самого
    // сокращения и никогда не срабатывал.
    pattern: new RegExp(
      `(?:несудим|о\\s+судимост|${NOT_LETTER_BEFORE}сон${NOT_LETTER_AFTER})`,
      'iu'
    ),
  },
  {
    authority: 'education',
    // Диплом и аттестат идут в комитет только С ПРИЛОЖЕНИЕМ; справки и
    // сертификаты из ВУЗов — наоборот, в Минюст. Это разные ветки, поэтому
    // «справка из вуза» ниже перехватывается отдельно.
    pattern:
      /(?:диплом|аттестат|удостоверени[а-яё]*\s+о\s+переквалиф|(?:^|[^а-яё])дпо(?:[^а-яё]|$)|образовательн[а-яё]*\s+документ)/iu,
  },
  {
    authority: 'zags',
    // Сокращения обязательны: сотрудники и клиенты пишут «СОР», а не
    // «свидетельство о рождении». Список из тетради, с. 3: СОР — о рождении,
    // СОБ — о браке, СОРБ — о расторжении брака, СОС — о смерти,
    // СОПИ — о перемене имени, СОУО — об установлении отцовства/усыновлении.
    // Без них «как апостилировать СОР выданное в Москве» переставало
    // распознаваться — регрессия, пойманная существующим eval-набором.
    pattern: new RegExp(
      '(?:загс' +
        '|свидетельств[а-яё]*\\s+о\\s+(?:рожден|брак|растор|смерт|усыновл|перемен)' +
        '|справк[а-яё]*\\s+о\\s+рожден' +
        `|${NOT_LETTER_BEFORE}(?:сор|соб|сорб|сос|сопи|соуо)${NOT_LETTER_AFTER}` +
        ')',
      'iu'
    ),
  },
  {
    authority: 'min_justice',
    pattern:
      /(?:нотариальн[а-яё]*\s+(?:копи|перевод|документ|доверенност)|доверенност|мфц|судебн[а-яё]*\s+документ|налогов[а-яё]*|справк[а-яё]*\s+из\s+вуза|сертификат[а-яё]*\s+из\s+вуза)/iu,
  },
];

/** К какому органу относится документ из вопроса. `null` — не определили. */
export function classifyApostilleDocument(question: string): ApostilleAuthorityKey | null {
  for (const matcher of DOCUMENT_MATCHERS) {
    if (matcher.pattern.test(question)) return matcher.authority;
  }
  return null;
}

export type IssuingRegion = 'local' | 'other' | 'unknown';

const LOCAL_REGION =
  /(?:санкт\s*петербург|петербург|(?:^|[^а-яё])спб(?:[^а-яё]|$)|ленинградск|лен\.?\s*обл)/iu;

/**
 * «Другой регион», названный прямо. Предлога не требует: формулировка
 * однозначна сама по себе.
 */
const OTHER_REGION_EXPLICIT = /друг[а-яё]*\s+(?:регион|город|област)/iu;

/**
 * Города. Список закрытый намеренно: угадывать регион по любому топониму
 * опаснее, чем не угадать — ошибка здесь становится детерминированным отказом.
 */
const OTHER_CITY_STEM =
  '(?:москв|перм|нижн|новосиб|екатеринбург|казан|самар|ростов|краснодар|воронеж|челябинск|волгоград|саратов|тюмен|иркутск|омск|уф[аеы]|красноярск|барнаул|владивосток|хабаровск|мурманск|архангельск|калининград|сочи|ставропол|белгород|тул[аеы]|ярославл|владимир|иванов|вологд|псков|новгород|севастопол|крым|симферопол|грозн|махачкал)';

/**
 * Город засчитывается ТОЛЬКО после предлога места.
 *
 * Половина названий в списке — это ещё и русские имена и фамилии: Владимир,
 * Иванов. Без предлога фраза «справка о несудимости, выданная Иванову»
 * читалась как «документ из Иваново» и давала клиенту детерминированный отказ.
 * Воспроизведено на живом коде, не гипотеза. Предлог отсекает этот класс
 * целиком: «выданная Иванову» предлога не имеет, «выдана в Иваново» имеет.
 */
const OTHER_REGION_CITY = new RegExp(
  `(?<![А-Яа-яЁё])(?:в|во|из|со)\\s+${OTHER_CITY_STEM}`,
  'iu'
);

export function detectIssuingRegion(question: string): IssuingRegion {
  const local = LOCAL_REGION.test(question);
  const other = OTHER_REGION_EXPLICIT.test(question) || OTHER_REGION_CITY.test(question);
  // Названы оба — по одному вопросу не понять, где выдан документ, а где его
  // хотят подать. Молчим: неверный детерминированный ответ хуже уточнения.
  if (local && other) return 'unknown';
  if (other) return 'other';
  if (local) return 'local';
  return 'unknown';
}

export type TerritorialVerdict =
  /** Берём: территориальный признак не мешает. */
  | { kind: 'accept'; authority: ApostilleAuthority }
  /** Оригинал апостилировать здесь нельзя — документ выдан в другом регионе. */
  | { kind: 'reject_original'; authority: ApostilleAuthority }
  /** Не хватает данных для решения. */
  | { kind: 'unknown' };

/**
 * Единственное решение, которое принимает этот модуль: можем ли мы взять
 * документ на апостиль здесь.
 *
 * Решение выдаётся ТОЛЬКО когда известны оба слота. Kimi справедливо указал
 * главный риск такой таблицы: ошибка извлечения слота превращается из случайной
 * в детерминированную. Поэтому при любой неоднозначности — `unknown`, и вопрос
 * идёт обычным путём.
 */
export function resolveApostilleTerritoriality(
  question: string
): TerritorialVerdict {
  const key = classifyApostilleDocument(question);
  if (!key) return { kind: 'unknown' };
  const authority = APOSTILLE_AUTHORITIES[key];

  // У образовательного комитета признака нет — регион вообще не спрашиваем.
  if (!authority.territorial) return { kind: 'accept', authority };

  const region = detectIssuingRegion(question);
  if (region === 'unknown') return { kind: 'unknown' };
  return region === 'other'
    ? { kind: 'reject_original', authority }
    : { kind: 'accept', authority };
}
