# Handoff: Knowledge Librarian — answering engine hardening + self-improving loop

**Created**: 2026-05-31 15:46
**Mode**: proceed
**Repository**: Knowledge Librarian (Avrora translation bot) — `C:\dev\translation`
**Branch**: `master` (clean, everything merged)
**Production**: https://avrora-library-production.up.railway.app — Railway deploy `98a2cf44` (SUCCESS) = `master` `061b06c`

---

> ## ⏩ UPDATE 2026-05-31 (continued — the pause didn't happen)
> After this handoff was written, the user sent a live-bot screenshot + ran the Codex review,
> so work continued and shipped in **PR #9 (`a29c04b`), deployed `a4978a34`**:
> - **Clarification-flow A/B/C** fixed (escalation spam / typed-reply context loss / junk drafts).
> - **All 7 Codex findings** fixed (see `docs/reviews/2026-05-31-codex-review-request.md` → RESOLVED table).
> - Verified: tsc + build + unit 18/18 + eval 18/18 + functional prod checks (P1#2, P1#3, B).
>
> **Still-open follow-ups** (the "REMAINING WORK" below, minus what's now done):
> 1. **Content gaps** — China/Hague, МВД two-address, pricing (need the user as domain expert).
> 2. **Schema drift** — reconcile `DocumentRevision` (db push still unsafe).
> 3. **P2#4 dedup** — a partial-unique DB index is the robust version (normalized check already ships).
> 4. Continue the one-by-one question re-run via `scripts/ask.ts` (next: "СОР из СПб в Москве?").
> The Codex review (item 1 below) is **DONE**.

---

## GOAL

The Avrora Telegram bot (@avroratranslatebot) answers questions about apostille /
document legalization from a knowledge base built out of uploaded documents. Two
problems drove this session:

1. **Trust/transparency** — the bot wasn't always confident or correct, and it was
   unclear whether an answer came from the uploaded documents or from the LLM's
   general knowledge.
2. **Self-improvement** — when the bot is uncertain, it should still answer from
   general AI knowledge, BUT turn that answer into a draft question→answer rule and
   send it to the admin for approval/correction. On approval it's saved to the KB so
   the next identical question is answered confidently from the base.

Both are **DONE and deployed**. This handoff is to (a) let a fresh session continue
the remaining content work, and (b) act on Codex's review once it comes back.

## CURRENT STATE

**Status**: ~95% complete. All engine + loop CODE is shipped, merged to master,
deployed, and verified on live prod. Remaining work is **content** (needs a domain
expert) and **acting on a pending Codex review**.

What's working (verified):
- Scenario gate asks for clarification when needed (e.g. "как апостилировать СОР" →
  asks issuing region → then gives full KB answer with address/price/schedule).
- Source attribution is honest (`knowledge_base` vs `general_ai`).
- Self-improving loop closes end-to-end: low-trust answer → draft → super-admin taps
  ✅ → ACTIVE QAPair → next identical question answered from KB. Verified on prod with
  a synthetic `kg:approve` callback.
- Eval harness 18/18 green.

What's NOT done:
- Content gaps (China/Hague, МВД two-address contradiction, pricing) — needs the user
  as domain expert.
- Codex critical review (request written, not yet run) — see
  `docs/reviews/2026-05-31-codex-review-request.md`.
- Schema drift reconciliation (`DocumentRevision`).

## WORK COMPLETED

All merged to master across PR #1–#7 (`6ffeb1a`..`061b06c`). Full per-PR breakdown is
in `docs/reviews/2026-05-31-codex-review-request.md`. The headline items:

- `src/lib/ai/enhanced-answering-engine.ts` — honest `answerSource`; honest confidence
  (`bestSemanticScore + coverageScore*0.1`); primary doc by aggregate semanticScore;
  consistency gate (`verifyAnswer`) checks chunks+rules+QA; **per-term keyword prefilter
  for BOTH rules (Step 5) AND qaPairs (Step 6)** — Step 6 was the bug that broke the loop
  (a freshly-approved QAPair fell out of an un-prefiltered `take:100`).
- `src/lib/ai/knowledge-feedback.ts` (NEW) — the loop: `isLowTrust`,
  `createKnowledgeGapSuggestion` (returns AIQuestion id, dedup by exact OPEN question),
  `approveKnowledgeGap` (→ ACTIVE QAPair, AIQuestion→ANSWERED), `rejectKnowledgeGap`.
- `src/lib/knowledge/scenario-classifier.ts` — glossary expansion; doc-type routing
  (notary/zags/opeka, `СО[РБС]`); general-requirement detector (lamination/юрлицо, uses
  `[а-я]*` NOT `\w*` because `\w` doesn't match Cyrillic in JS); destination-country
  detector ("апостиль для/в <country>" with negative lookahead) → knowledge_lookup.
- `src/lib/knowledge/glossary.ts` (NEW) — data-driven `expandAbbreviations()`.
- `src/lib/telegram/ai-escalation.ts` — hook at top of `escalateUnconvincingAIAnswer`:
  create draft → send super-admins inline ✅/✖️ buttons → early return.
- `src/lib/telegram/knowledge-gap-callback.ts` (NEW) — `kg:approve|reject:<id>`,
  super-admin gate.
- `src/lib/telegram/message-router.ts` — routes `kg:` callbacks (before `sc:`).
- `src/app/api/ai-questions/[id]/route.ts` — PATCH actions `approve`/`reject`.
- `src/app/admin/ai-questions/page.tsx` — web review UI (edit draft answer, approve/reject,
  "🆕 Черновик правила" badge).
- `src/lib/document-processing/extraction-lint.ts` (NEW) + `commit.ts` — quality gate on
  extracted rules at commit time.
- `prisma/schema.prisma` — `QAPair.metadata Json?` (applied via raw ALTER, see DATABASE).
- `scripts/eval/run.ts` + `cases.json` (NEW) — 18-case deterministic regression gate.
- `scripts/ask.ts` (NEW) — single-question provenance harness (use this to re-run test Qs).
- `scripts/diagnose-answer.ts` — per-question try/catch.

### Commits Made (this session)
- `061b06c` Merge PR #7 — self-improving knowledge loop
- `0981506` fix(ai): keyword-prefilter Q&A pairs too (close the feedback loop)
- `5977205` feat(admin): web review + approve/edit/reject knowledge-gap drafts
- `c177c2a` feat(telegram): approve/reject drafts via inline buttons
- `445e1db` feat(ai): wire draft trigger + web approve/reject API
- `6313abe` feat(ai): knowledge-gap draft builder + approve/reject
- `374a109` feat(db): QAPair.metadata for provenance
- `c8302a2` PR #6 country-destination routing
- `adaf347` PR #5 answer-quality v2
- `76c456a` PR #4 answer-engine robustness (eval harness, glossary, ingest gate, provenance)
- `07e4931` PR #2 СОБ/СОС abbreviations
- `e3af4f5` PR #3 answer-engine tech debt
- `0b872ba` PR #1 semantic source attribution + escalate general_ai

### Uncommitted Changes
- `scripts/ask.ts` (renamed from `_ask.ts`, documented) — about to be committed with this handoff.
- `docs/reviews/2026-05-31-codex-review-request.md` — Codex review prompt.
- this handoff file.

## ARCHITECTURE & FLOW

```
question → classifyScenario() [gate BEFORE retrieval]
   ├ scenario_clear → retrieve within scenario
   ├ needs_clarification → ask (sc:<id> buttons)   ← not an answer
   ├ knowledge_lookup → general retrieval
   └ out_of_scope → general_ai / "нет данных"
→ hybrid retrieval (semantic+keyword RRF): Step5 rules, Step6 qaPairs (both per-term prefilter)
→ LLM compose → verifyAnswer() strips unsupported claims
→ confidence = bestSemanticScore + coverageScore*0.1
→ if low-trust (general_ai|low|insufficient & not clarification):
      escalateUnconvincingAIAnswer → createKnowledgeGapSuggestion → AIQuestion(knowledge_gap,OPEN)
         → super-admins get ✅/✖️ buttons (kg:approve:<id>/kg:reject:<id>)

APPROVAL: super-admin taps ✅ (TG) or web PATCH {action:'approve'}
   → approveKnowledgeGap: guard issueType+status==OPEN → create ACTIVE QAPair
     {metadata:{origin:'ai-suggested',approvedBy,approvedAt,fromAIQuestion}} → AIQuestion→ANSWERED
   → next identical question retrieves the new QAPair → answerSource=knowledge_base
```

## KEY FILES

| File | Purpose | Key functions |
|------|---------|---------------|
| `src/lib/ai/enhanced-answering-engine.ts` | core answer pipeline | `answerQuestionEnhanced`, `verifyAnswer`, Step5/Step6 retrieval |
| `src/lib/ai/knowledge-feedback.ts` | self-improving loop | `isLowTrust`, `createKnowledgeGapSuggestion`, `approveKnowledgeGap`, `rejectKnowledgeGap` |
| `src/lib/knowledge/scenario-classifier.ts` | gate before retrieval | `classifyScenario` |
| `src/lib/knowledge/glossary.ts` | abbreviation expansion | `expandAbbreviations` |
| `src/lib/telegram/ai-escalation.ts` | trigger + notify admins | `escalateUnconvincingAIAnswer`, `sendKnowledgeGapForApproval` |
| `src/lib/telegram/knowledge-gap-callback.ts` | TG approve/reject | `handleKnowledgeGapCallback` |
| `src/app/api/ai-questions/[id]/route.ts` | web approve/reject API | PATCH |
| `src/app/admin/ai-questions/page.tsx` | web review UI | `handleAction` |

## DATABASE

PostgreSQL on Railway. Migrations via `railway run npx prisma db push` — **BUT db push
is currently UNSAFE** (see below).

| Table | Relevance | Changes |
|-------|-----------|---------|
| `QAPair` | stores approved Q→A pairs (incl. ai-suggested) | **added `metadata Json?`** via raw ALTER |
| `AIQuestion` | holds drafts (`issueType='knowledge_gap'`, `context.draft`) | no schema change, new issueType value |
| `DocumentRevision` | pre-existing | **schema drift**: present in DB, not clean in schema history |

### Migrations
- `QAPair.metadata` applied via `ALTER TABLE "QAPair" ADD COLUMN IF NOT EXISTS "metadata" JSONB;`
  (`prisma.$executeRawUnsafe`) + `prisma generate`. The schema.prisma is updated to match.
- **DO NOT run `prisma db push`** — it wants to DROP `DocumentRevision` (3 rows) because of
  drift. Use surgical ALTER for any new column until the drift is reconciled. Reconciling the
  drift is a clean follow-up task (introspect with `prisma db pull`, diff, decide).

## PHILOSOPHY & DECISIONS

| Decision | Rationale | Rejected |
|----------|-----------|----------|
| Human ALWAYS gates the loop | AI draft answer can be wrong; never auto-save | auto-saving high-confidence general_ai |
| Scenario gate BEFORE retrieval | one clarifying Q beats a confidently-wrong answer | retrieve-then-guess |
| Per-term keyword prefilter + recent-N fallback | exact `contains` recall for Cyrillic terms; `take:100` alone dropped fresh rows | pure semantic top-K |
| `[а-я]*` not `\w*` in RU regexes | JS `\w` excludes Cyrillic — silent miss | `\w*` (the bug we hit twice) |
| Deterministic eval (no LLM judge) | cheap, stable regression gate | LLM-as-judge (flaky, costly) |
| Raw ALTER not db push | db push would drop DocumentRevision | `--accept-data-loss` (forbidden) |
| Test prod via Node fetch, never curl | Windows Git Bash curl corrupts Cyrillic UTF-8 → bot gets "???" → false out_of_scope | curl with `--data` |

## REMAINING WORK

### Priority Order:
1. **Run the Codex review** — hand `docs/reviews/2026-05-31-codex-review-request.md` to
   Codex. It lists 7 specific risk areas (loop safety, recall, Cyrillic regexes, consistency
   gate, dedup races, confidence formula, migration drift). Triage findings P0→P3; fix P0/P1
   before anything else. Re-run `scripts/eval/run.ts` after each fix.
2. **Content gaps (needs the USER as domain expert — ASK, don't guess)**:
   - **China / Hague** — Hague Apostille Convention effective for China 2023-11-07. KB has no
     China entry. Get the correct current procedure from the user, add as QAPair/rules.
   - **МВД two-address contradiction** — KB says both "Красного текстильщика 10-12" AND
     "Литейный 6". Find which is correct (or which service maps to which), fix the source.
   - **Pricing** — some answers quote госпошлина, some quote full-service price. Decide one
     source of truth per fact and align.
3. **Reconcile schema drift** — `prisma db pull` → diff `DocumentRevision` against schema →
   make schema.prisma authoritative → restore safe `db push`. Do NOT drop data.
4. **Continue the one-by-one question re-run** (the user was mid-flow): use
   `railway run npx tsx scripts/ask.ts "<question>"`. Next queued question was
   "можно ли апостилировать СОР, выданное в СПб, в Москве?". Describe each answer in PLAIN
   language: source (KB vs general AI), confidence, whether it asked for clarification.
5. **Optional**: re-enable Beads for tracking (none used this session).

## HOW TO VERIFY / RUN

```bash
# Single question with provenance (the user wants plain-language summaries of each):
railway run npx tsx scripts/ask.ts "как апостилировать СОР"

# Regression gate — must stay 18/18:
railway run npx tsx scripts/eval/run.ts

# Confirm prod parity:
git rev-parse --short HEAD            # should equal remote master
railway deployment list | head -3     # latest = SUCCESS

# Deploy (auto-deploy is unreliable — force it):
railway up --detach
```

## GOTCHAS (do not relearn the hard way)
- **Never curl Cyrillic on Windows Git Bash** — UTF-8 corruption → bot sees "???". Use
  `scripts/ask.ts` / Node fetch.
- **Never `prisma db push`** until drift reconciled — it drops `DocumentRevision`.
- **Never kill processes / ports** (global rule). Port busy → +13 and retry.
- **PR workflow** — feature branch → PR, never push master directly (except `.claude/` & CLAUDE.md).
- **Railway transient DB blips** ("Can't reach database server at shinkansen.proxy.rlwy.net")
  are network, not code — just retry.
