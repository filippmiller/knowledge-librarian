# Handoff: Aurora — baseline почти получен, упёрлись в 403 аккаунта Anthropic

**Created**: 2026-08-12 ~16:00 (Kimi Code CLI, сессия после Claude)
**Mode**: proceed
**Repository**: `C:\dev\translation`, ветка `feat/canonical-docx-blocks`, HEAD = `761a84c`, дерево чистое
**Предыдущий хендофф**: `.claude/handoffs/2026-08-12-153000.md` — читать ОБЯЗАТЕЛЬНО первым (цели, архитектура, философия, ошибки, полный план). Ниже только дельта этой сессии.

---

## БЛОКЕР (главное)

**Anthropic API возвращает `403 forbidden: Request not allowed` на ЛЮБОЙ вызов** — проверено минимальным запросом (10 токенов, "ping") вне пайплайна. Это не код и не контент фикстуры — блок на стороне аккаунта. Началось ~15:30 локального посреди baseline-прогона (Q01 успел ответиться, дальше fetch failed → 403).

**Действие владельца**: console.anthropic.com → Billing (баланс/spend limit) → API Keys (ключ активен?) → Usage/Limits.

Проверка одной командой (не печатать ключ!): минимальный POST на `https://api.anthropic.com/v1/messages` с `x-api-key` из `.env` (`ANTHROPIC_API_KEY`), model `claude-sonnet-4-5`, max_tokens 10. 403 → ждать дальше; 200 → снимать блокер и продолжать.

В сессии Kimi висела cron-задача `f99cf47d` (пинг API каждые 11 мин с автопродолжением) — после перезагрузки компа сессия может не подняться; при новом старте просто проверь API вручную и иди по плану ниже.

## ЧТО СДЕЛАНО В ЭТОЙ СЕССИИ (дельта)

1. Прочитан и разобран лог прошлой сессии + хендофф 15:30. Анализ для владельца: узкое место — не extraction-промпты, а resolution/applicability + негативные кейсы; методология — сначала accepted baseline, потом фиксы (владельцу дан план из 5 пунктов, он его видел).
2. **REMAINING #1 выполнен частично**: `scratchpad/baseline/` — полный прогон с `dependencyGraphStage: REQUIRED` дошёл до конца. Граф не сошёлся → **деградировал до изолированных точек с записью в журнал** (двери `8969d4a` работают как задумано — прогон не умер).
3. Но questions-стадия того прогона сломалась инфраструктурно: **1 DIRECT_ANSWER, 15 ERROR** (`fetch failed` ×13, timeout ×1, 403 ×1). Грейд `scratchpad/diag/baseline.json` = 1/10 + 0/6 — **не показателен, это не качество движка, а сеть/блок**.
4. Повтор questions на замороженном артефакте (`scratchpad/baseline-retry/`) — все 32 вызова 403, $0.00. Подтверждено: **fingerprint-защита `--reuse-extraction` работает**, дешёвая итерация механически исправна; 403 корректно классифицирован как non-retryable (ретрай-шторма не было).

## СЛЕДУЮЩИЕ ШАГИ (после оживления API)

```bash
# 1. Questions на замороженном артефакте (граф REQUIRED — деградирует сам, ~$1)
npx tsx scripts/run-aurora-fixture.ts --mode=e2e \
  --reuse-extraction=scratchpad/baseline/run-1-2026-08-12T12-28-58-045Z/extraction-artifact.json \
  --out=scratchpad/baseline-retry2 --max-cost-usd=2.00 --max-paid-calls=300

# 2. Грейд (бесплатно) — это и есть ПЕРВЫЙ ПРИЁМОЧНЫЙ baseline
npx tsx scripts/grade-aurora-fixture.ts --in=scratchpad/baseline-retry2 \
  --out=scratchpad/diag/baseline2.json --accept-degraded-run
```

3. **REMAINING #2 — замер флака**: 3 прогона questions-only на неизменном коде на том же артефакте (`--reuse-extraction`, ~$0.62 каждый). Без этого ни один фикс не атрибутируем.
4. Дальше по приоритетам хендоффа 15:30: #3 контрольный замер «весь корпус в промпте» → #4 contextual retrieval → #5 поэтапные метрики → #6 каталог фактов (translation-0lr, батчем) → #7 переезд на таблицу по апостилю (мишень приёмки владельца!).

## КЛЮЧЕВЫЕ АРТЕФАКТЫ

| Путь | Что |
|---|---|
| `scratchpad/baseline/run-1-2026-08-12T12-28-58-045Z/extraction-artifact.json` | ЗАМОРОЖЕННОЕ извлечение, точка дешёвых итераций |
| `scratchpad/diag/baseline.json` | Грейд сломанного сетью прогона (1/10 — игнорировать) |
| `scratchpad/questions-first-score/` + `scratchpad/measure-3` | Два не-приёмочных замера (6/10+0/6 и 4/10+3/6), разницу НЕ атрибутировать |
| `scripts/fixtures/semantic-rule-extraction-test-pack/` | Оракул: `kontrolnye_voprosy_i_klyuch.md`, `test_cases_semantic_rules.jsonl` |

## НАПОМИНАНИЯ ИЗ РАЗБОРА (владелец в курсе)

- Не оптимизировать прокси (аудиторов extraction) — только счёт грейдера.
- n=1 прогон ничего не доказывает; флейк не измерен.
- Тесты «промпт содержит строку» — не защита; нужен replay-слой.
- Мишень приёмки — таблица решений по апостилю (trainee-notebook), фикстура про почёсывание вторична.
- `translation-0lr` (P0): пять trigger-фактов + десять фасет зашиты в код — блокер портируемости на станки/переписку; чинить батчем, правка реестра инвалидирует замороженный артефакт.
- Неизвестные дефекты вопросной стороны: `translation-me6`, `translation-5zk`, `translation-6mh` (последний подтверждён живьём, фикс неполон).
