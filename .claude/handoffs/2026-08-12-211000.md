# Handoff: Aurora — retrieval оправдан и снят с приоритета; узкое место — resolution/applicability

**Created**: 2026-08-12 ~21:10
**Mode**: proceed
**Repository**: `C:\dev\translation`, ветка `feat/canonical-docx-blocks`, HEAD = `d6b27f5`, дерево чистое
**Предыдущие хендоффы**: `2026-08-12-153000.md` (ОБЯЗАТЕЛЕН: цели, архитектура, философия), `2026-08-12-173500-kimi.md` (baseline 8/16, флак 0, эталон 15/16). Ниже только дельта.

---

## ГЛАВНЫЙ РЕЗУЛЬТАТ: приоритет #4 из прошлого хендоффа ОТМЕНЁН замером

Прошлый хендофф назначил **contextual retrieval главным приоритетом**, рассуждая: раз эталон даёт 15/16, а пайплайн 8/16, и среди падений есть 2 RETRIEVAL_MISS + 2 «возможно следствие неполного пула» — значит retrieval первый подозреваемый.

**Это опровергнуто.** Не рассуждением, а замером.

### Бесплатная часть (стоимость $0, по уже оплаченным артефактам)

Грейдер УЖЕ считает постадийную воронку (`missingFromExtraction/Candidates/TopK/Selected`, `grade-aurora-fixture.ts:179-187`). Никто её не читал. Раскладка 8 падений baseline:

| Стадия | Кейсы |
|---|---|
| RETRIEVAL_MISS | Q02, Q05-N1 |
| RESOLUTION_ERROR | Q03, Q10 |
| APPLICABILITY_ERROR | Q09-N1, Q10-N1 |
| SYNTHESIS_ERROR | Q09 |
| EVIDENCE_ERROR | Q04-N1 |

То есть retrieval владел **2 из 8**, а не 4. Плюс, по RRF-трассе (она есть в `engine-results.json[].retrieval.trace` на все 49 юнитов):

```
корпус 49 юнитов → RRF → fused.slice(0, 20) ← отрез
Q02:    правило 2 на RRF-рангах 24, 29, 32, 34, 35
Q05-N1: правило 3 на RRF-рангах 29, 33, 35, 43
recall@20 = 89.5%   recall@30 = 100%
```

Детерминировано во всех трёх оплаченных прогонах (baseline-retry2 ×2, flake-3).

### Платная часть: замер ПОТОЛКА ($1.13, 2 прогона вопросов)

Вместо того чтобы подбирать константу пула под recall@30 этой фикстуры (**это была бы подгонка под фикстуру** — ровно тот грех, который проект считает худшим), снят потолок: пул = всему корпусу (49). Он отвечает на вопрос, на который никакой дизайн не отвечает — **купит ли допуск этих правил в пул сами кейсы**.

Артефакты: `scratchpad/pool-ceiling-49/`, грейд `scratchpad/diag/pool-ceiling-49.json`.

**Ответ: НЕТ. 7/16 против 8/16.**

Q02 и Q05-N1 провалены и при пуле = всему корпусу. Они лишь СМЕНИЛИ стадию:

| Кейс | пул 20 | пул 49 |
|---|---|---|
| Q02 | RETRIEVAL_MISS | RESOLUTION_ERROR (`resourceAvailability_unknown`, `exception_trigger_unknown`) |
| Q05-N1 | RETRIEVAL_MISS | APPLICABILITY_ERROR (правило 3 не выбрано, правило 5 выбрано ошибочно) |

Retrieval был **маскирующим** дефектом: убрали — обнажился следующий слой. Contextual retrieval не купил бы эти кейсы: правила уже ранжировались на 24 и 29 из 49, и допуск их в пул ничего не исправил. Улучшать ранжирование там, где ранжирование уже достаточное, — потраченные дни.

### ПОПРАВКА к формулировке выше (найдена ревью Grok 4.6)

Счёт 8/16 → 7/16 — это НЕ «чистое нет». Это **размен**: −2 кейса, +1 кейс.

**Q04-N1 на пуле 49 ПРОХОДИТ** (обоих прогонах; на пуле 20 падал с `EVIDENCE_ERROR`, «selected evidence misses required numeric(s): 15 seconds», это `translation-me6`). Первая редакция этого хендоффа упоминала только регрессии и умалчивала выигрыш — счёт −1 подразумевает +1−2, но +1 не был назван.

Это меняет интерпретацию: размен — довод ЗА посылку `translation-6rr` (судьба кандидата зависит от состава пула), а не только против retrieval.

### Побочный результат: translation-6rr подтверждён живьём

Пул 20→49 на ТОМ ЖЕ коде и ТОМ ЖЕ замороженном артефакте уронил два кейса, которые проходили:

- **Q04** → `ENGINE_ERROR`: «Challenge verifier returned inconsistent verdict for 0b046081cc537a39: AMBIGUOUS but checks require CONFLICTS». Новый бид **`translation-69l` (P1)**. Воспроизводится в обоих прогонах.
- **Q08** → RESOLUTION: «rule 8 in top-K but not selected».

Ожидаемые правила Q04 и Q08 были в пуле и на 20, и на 49 — их ранги не менялись. Регрессия вызвана ИСКЛЮЧИТЕЛЬНО появлением 29 новых соседей в совместном listwise-вызове реранкера. Это прямое доказательство механизма `translation-6rr`, а не догадка. Замечания записаны в сам бид.

### Цена как аргумент

Реранкер на пуле 49 — **$0.654 из $1.126** прогона (58%). На тысячах юнитов реального корпуса реранжировать всё нельзя ни по цене, ни по масштабу. Пул 49 был диагностическим потолком, а не кандидатом в конфигурацию. **Дефолт НЕ менялся.**

---

## КУДА НА САМОМ ДЕЛЕ УШЛИ 7 КЕЙСОВ РАЗРЫВА С ЭТАЛОНОМ

Убрав retrieval как фактор (пул 49), получаем чистую картину узкого места:

| Стадия | Кейсов |
|---|---|
| RESOLUTION_ERROR | 4 (Q02, Q03, Q08, Q10) |
| APPLICABILITY_ERROR | 3 (Q05-N1, Q09-N1, Q10-N1) |
| SYNTHESIS_ERROR | 1 (Q09) |
| ENGINE_ERROR | 1 (Q04) |

**7 из 9 падений — resolution/applicability.**

### ДВЕ ПОПРАВКИ К ЭТОМУ ВЫВОДУ (ревью Grok 4.6) — читать до того, как на него опираться

**1. Таблица выше ЗАГРЯЗНЕНА.** Пул 49 — это не «retrieval убран», а «убран отрез RRF, а реранкеру скормлен весь корпус». Ниже по течению изменилось всё: оценки listwise, полоса отбора, decision-relevance, набор challenge. Именно поэтому перевернулись Q04 и Q08. Считать эту таблицу чистой картиной resolution нельзя.

**2. Связка Q02 с `translation-0lr` НЕВЕРНА.** Q02 падает с `resourceAvailability_unknown` — это **не** `UNRECOGNIZED_TRIGGER_CONDITION`. Каталог факт представил; резолвер отказался отвечать на вопрос о видимой чистоте ладоней, пока не выяснит наличие воды. Это дефект избыточной атрибуции + известная политика резолвера, а не закрытость каталога. Каталог-как-данные это НЕ чинит; каталог ПОБОЛЬШЕ сделает такие отказы ЧАЩЕ (больше неизвестных фактов, на которых можно уточняться).

`translation-0lr` остаётся реальной проблемой портируемости. Но этот замер её не доказал.

---

## WORK COMPLETED

### Коммиты сессии

- `00ac341` — эталон «весь корпус в промпте» + хендоффы Kimi (её незакоммиченная работа)
- `ebd3310` — `--rerank-pool-size` как диагностическая ручка + `retrievalContractProbe(effective)`
- `b2ebb1c` — беды: подтверждение `translation-6rr`, новый `translation-69l`
- `d6b27f5` — правки по независимому ревью Grok 4.6

### Что изменено в коде

- `scripts/run-aurora-fixture.ts` — флаг `--rerank-pool-size=N`, опциональный. Отсутствует → поведение байт-в-байт прежнее (доказано тестом на совпадение отпечатков). Действующий размер читается из ОДНОГО локального `effectiveRerankPoolSize`, который питает и пробу отпечатка, и вызов `retrieveUnits`. Поле `rerankPoolSizeOverride` в `run-summary.json` появляется только при переопределении.
- `src/lib/knowledge/semantic-retrieval.ts` — `retrievalContractProbe(effectiveRerankPoolSize?)`. Раньше проба ВСЕГДА рапортовала константу модуля, поэтому прогон с переопределённым пулом получал отпечаток, неотличимый от дефолтного, и два несопоставимых замера молча слились бы в один.
- Тесты: `scripts/__tests__/run-aurora-fixture-rerank-pool-size.test.ts` (новый), `src/lib/knowledge/__tests__/semantic-retrieval.test.ts` (+3).

`tsc --noEmit` чист, `vitest` **1979/1979** (было 1966).

### Ревью Grok 4.6

Прогнан на диффе (`grok -m grok-4.6 --effort high`). Вердикт «approve» с двумя незаблокирующими находками — обе исправлены в `d6b27f5`:
1. проба и вызов читали значение независимо → могли разойтись при будущей правке;
2. `run-summary.json` не фиксировал переопределение.

**Замечание по скилу `grok`**: его проверка версии устарела. Скил утверждает «xAI = 0.2.x, npm-подделка = 1.0.1»; на машине сейчас **xAI CLI 1.0.3**, и `grok models` показывает `grok-4.6` (дефолт) и `grok-4.5`. Отличать по `grok models`, а не по номеру версии.

---

## REMAINING WORK

### Priority Order (пересобран после ревью Grok 4.6 — обоснование в разделе выше)

1. **`translation-69l` (P1) — challenge verifier.** ПЕРВЫМ не потому что мельче, а потому что **сломан инструмент**: `ENGINE_ERROR` — это не «неверный ответ», это отсутствие ответа, то есть нечитаемая клетка в любом последующем замере A/C.

   Починка известна и структурна. `challenge-compatibility.ts:110-115` УЖЕ выводит вердикт детерминированно:
   ```ts
   const expectedVerdict = hasYes ? 'CONFLICTS' : hasUnknown ? 'AMBIGUOUS' : 'COMPATIBLE';
   ```
   …и тут же бросает, если параллельное поле `verdict`, запрошенное у модели, с этим разошлось. **Перестать спрашивать `verdict` у модели** — выводить из двух checks. Тогда рассогласование невозможно по конструкции (тот же приём, что применён к отпечатку пула в `d6b27f5`).

   Стоимость проверки: юнит-тест деривера на записанном payload Q04 (`0b046081cc537a39`) — **$0**, трасса на диске. Затем один questions-only прогон на дефолтном пуле — **~$0.60**.

2. **КОНТРОЛЬ, КОТОРОГО НЕТ: «извлечённые ЮНИТЫ в промпте».** Самый дешёвый нерастраченный эксперимент, **~$0.05-0.10**.

   Существующий эталон 15/16 кормит модель **сырыми каноническими блоками** (`run-full-corpus-control.ts:244-246`, `extractCanonicalDocument`), а НЕ извлечёнными юнитами. Значит разрыв 15/16 против 8/16 смешивает потери извлечения с потерями конвейера, и приписывать остаток resolution/applicability — пока гипотеза.

   Скормить все 49 извлечённых юнитов в промпт, минуя RRF/реранкер/резолвер:

   | Результат | Что значит |
   |---|---|
   | ~8/16 | проблема в ЮНИТАХ. Резолвер не трогать, идти в извлечение |
   | ~15/16 | решающие стадии губят хорошие юниты → C и политика резолвера |
   | ~11-13/16 | и то и то; перестать говорить «узкое место» в единственном числе |

3. **Опционально, ~$0.35-0.45: аблация `--rerank=off`.** Реранкер съел 58% стоимости прогона на пуле 49 и он же — источник нестабильности `6rr`. Прогон на порядке RRF без listwise-вызова отвечает «окупается ли стадия вообще». Если 8/16 держится — C превращается из «жить с нестабильным судьёй» в «удалить совместный порог».

4. **`translation-0lr` — ТОЛЬКО как A-lite, и не под Q02.** Закрытый список оставить (причина настоящая: общий язык извлечения и запроса). Менять ИСТОЧНИК: грузить `TRIGGER_FACT_REGISTRY`/`FACET_REGISTRY` из JSON на корпус/арендатора, строить Zod и промпты из файла, закоммитить каталог апостиля, файл про почёсывание оставить фикстурой.

   **НЕ строить** «извлекать свободно → эмбеддинги → кластеризация синонимов → LLM разрешает споры», пока нет ДВУХ реальных корпусов, которые реально столкнулись. Это многодневный платный цикл, инвалидирующий точку заморозки, и Q02 он не чинит.

   Оговорка к формулировке бида «меняется только источник словаря»: это не так. `queryFactMapSchema` — пять полей руками, `QueryFactMap` — mapped type, три промпта рендерят реестр, `z.enum` компилируется. Нужен динамический Zod + рендеринг промптов. Выполнимо, но это не подмена источника.

5. **`translation-6rr` (C) — ПОСЛЕ пункта 2**, и только если контроль скажет, что течёт на решающих стадиях. Форма фикса правильная (цитирование и проверка применимости не должны делить `max(0.25, best*0.4)`), но делать её сейчас, при открытом `6mh` на том же пуле, — снова подгонка под фикстуру. Пул 49 уже показал цену: **купили Q04-N1, заплатили Q04 и Q08**.

6. **Переезд на таблицу по апостилю** — мишень приёмки владельца (память `trainee-notebook-source`). Честный продуктовый шаг — оракул по апостилю, а не лучший каталог для фикстуры про почёсывание: на этой фикстуре все 10 доменных фасет висят UNKNOWN, поэтому дальнейшая работа по ней НЕ переносится на апостиль.

**Contextual retrieval — снят на этой фикстуре, но НЕ как продуктовое суждение.** Ранги 24 и 29, допуск всех не помог, recall@20 = 89.5% на 49 юнитах — не аварийная ситуация. Но следующий корпус — таблица решений, где контекст строки/ячейки/заголовка снова делает это задачей retrieval, и recall@20 на 49 юнитах ничего не говорит о 400-2000. Припарковать, не удалять; вернуть, когда recall@20 на апостильном артефакте окажется плохим.

**Contextual retrieval (`#4` прошлого хендоффа) — СНЯТ с приоритета.** Не «сделано», а «измерено как не окупающееся сейчас». Вернуть в рассмотрение, когда resolution/applicability перестанут быть узким местом, ИЛИ когда корпус вырастет настолько, что recall@20 просядет по-настоящему (на 49 юнитах он 89.5% и лечится не эмбеддингами).

### Инструмент, который стоит переиспользовать

Скрипт recall@K по RRF-трассе жил в scratchpad и НЕ закоммичен (`scratchpad` в `.gitignore`). Если постадийные метрики (`#5` прошлого хендоффа) будут доводиться — начинать с него: он читает `engine-results.json[].retrieval.trace` и оракульные `expected_rule_ids`, считает ранг каждого ожидаемого правила и кривую recall@K, стоит $0 и детерминирован. Именно он за минуты дал вывод, ради которого иначе потратили бы дни.

---

## НАПОМИНАНИЯ (всё ещё в силе)

- Не оптимизировать прокси — только счёт грейдера. Не подгонять метрику. Константа пула, подобранная под recall@30 этой фикстуры, была бы подгонкой.
- Правило атрибуции (из прошлого хендоффа): фикс засчитывать только если меняется НАБОР падающих кейсов; сдвиг счёта на ±1 без смены набора — шум. Verdict-флак измерен = 0/48.
- `translation-6mh` (правило 9 в operative, Q09-N1) — фикс неполон, подтверждён живьём уже 4-й раз (воспроизвёлся и на пуле 49).
- Резолвер `resolution.ts` ~570-655: наивный фикс «CONDITIONAL → CLARIFY» сломает Q05/Q09 — проверено замером, не повторять.
